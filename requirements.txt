fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# OpenCV — contrib variant satisfies mediapipe's bundled dep;
# headless variant is NOT used so both packages are not present simultaneously.
opencv-contrib-python==4.10.0.84
numpy==1.26.4

# Face detection (Tasks API — mediapipe 0.10.35 supports Tasks only, NOT solutions.pose)
mediapipe==0.10.35

# Human Emotion Detection — HSEmotion (EfficientNet-based)
hsemotion==0.3.0
timm==0.9.*
pillow>=9.0.0

# CPU-only PyTorch — saves ~1.5 GB RAM on Render vs full CUDA build
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# Audio Emotion Detection — Groq API
groq>=0.9.0

# Animal Emotion Detection — Hugging Face ViT
transformers==5.5.0

#my name is 
